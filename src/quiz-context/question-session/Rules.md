Business rules:
- When student accesses lobby, he must either:
  - If no session is active, stay in lobby (waiting room)
  - if 1 session is active, he should directly be redirected and be asked to answer question
  - if more than 1 session is active, he should be redirected to the one first the closest endDate
